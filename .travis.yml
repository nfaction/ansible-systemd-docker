---
dist: xenial
sudo: required

language: python

services: []

before_install:
  - deactivate
  - sudo apt-get update -qq

install:
  - sudo pip install molecule==2.19 ansible==2.7.11
  - ansible --version
  - molecule --version

script:
  - molecule test
